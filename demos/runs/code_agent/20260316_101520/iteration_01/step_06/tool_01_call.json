{
  "tool_call_id": "call_fefedd3933f84f598977575a",
  "function": "write_file",
  "arguments": {
    "path": "solution.py",
    "content": "import re\nfrom collections import Counter\nfrom typing import List",
    "extract_emails(text": "str) -> List[str]:\n    \"\"\"\n    Extract valid email addresses from text using proper regex pattern.\n    \n    Args:\n        text: Input text string to search for email addresses\n        \n    Returns:\n        List of valid email addresses found in the text\n        \n    Examples:\n        >>> extract_emails(\"Contact me at john@example.com or jane@test.org\")\n        ['john@example.com'",
    "jane@test.org": "n    \"\"\"\n    if not isinstance(text",
    "str)": "n        raise TypeError(\"Input must be a string\")\n    \n    # Regex pattern for email validation (RFC 5322 compliant simplified version)\n    email_pattern = r'\\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Z|a-z]{2"
  }
}